pipeline {
    agent any

    stages {
        stage('Install Dependencies') {
            steps {
                bat 'cd "%WORKSPACE%" && npm install'
            }
        }

        stage('Run Application') {
            steps {
                bat 'cd "%WORKSPACE%" && node main.js'
            }
        }
    }
}
