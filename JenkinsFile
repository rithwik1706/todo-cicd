pipeline {
    agent any

    stages {
        stage('Install Dependencies') {
            steps {
                sh 'npm install'
            }
        }
        stage('Run with PM2') {
            steps {
                sh '''
                pm2 restart myapp || pm2 start main.js --name myapp
                pm2 save
                '''
            }
        }
    }
}
